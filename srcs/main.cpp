#include "../includes/Webserv.hpp"

int	main(int argc, char *argv[])
{
	if (argc != 2)
	{
		// Rajout fonction cas error pour remplacer ceci
		std::cerr << "Error: One Argument valid!" << std::endl;
		std::exit(-1);
	}
	return (0);
}
